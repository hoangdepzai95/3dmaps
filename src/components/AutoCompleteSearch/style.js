import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 32,
    flex: 1,
  },
  input: {
    height: 32,
    borderWidth: 1,
    borderColor: '#ccc',
    borderStyle: 'solid',
    borderRadius: 3,
    padding: 7,
  },
});

export default styles;
