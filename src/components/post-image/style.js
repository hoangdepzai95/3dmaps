import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  favoriteIcon: {
    tintColor: 'white',
    top: 10,
    right: 30,
  },
});
export default styles;
