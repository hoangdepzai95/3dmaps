import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    flex: 1,
    position: 'relative',
  },
  favoriteIcon: {
    tintColor: 'white',
    position: 'absolute',
    top: 10,
    right: 10,
  },
});
export default styles;
